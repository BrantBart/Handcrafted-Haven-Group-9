import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['encrypted-tbn1.gstatic.com', "img.freepik.com"],
  },
  // You can add any other configurations you might need here
};

export default nextConfig;
